package in.wynk.secret.manager.rules.basic.enums;

/**
 * Enum representing different match strategies.
 */
public enum MatchType {
    EXACT,
    RANGE,
    FUZZY,
    PREFIX,
    SUFFIX,
    SUBSTRING,
    CASE_INSENSITIVE;
}
